

export const ByType = ({ data }) => {
    const type = data.type;
    console.log(type);
    return (
        // const filtredData = data.filter((el) => { el === 

        // })
        null
    );
}